title: 'Experimentos '
description: Revisar aplicación para futuras animaciones
template:
  repo: experimentos-template
  name: experimentos
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
